# 社区慢病随访用药提醒与家庭血压平台
# 复制为 .env 后按需修改；compose 会自动读取

# ---- 数据库（仅 compose 内部网络使用，不发布到宿主） ----
DB_NAME=chronic
DB_USER=chronic
DB_PASSWORD=chronic

# ---- 应用发布端口（宿主机）----
# 验证环境由平台注入 CC_PUBLISH_PORT；本地部署可改为 8080 等
CC_PUBLISH_PORT=8080

# ---- 是否写入演示账号与演示数据（首次启动时） ----
SEED_DEMO_DATA=true
